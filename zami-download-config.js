/*
  ZAMI APP DOWNLOAD CONFIG
  ------------------------
  Edit ONLY the line below. Everything else (the popup, the download
  page) reads from this file, so you only have to update the link
  in one place whenever you publish a new APK build.

  Your zami.apk file lives directly in the same GitHub Pages repo as
  browse.html, download.html, etc. — so it's already publicly served
  at this Pages URL, no GitHub Release needed. Whenever you upload a
  new zami.apk to the repo (same filename, same folder), it overwrites
  the old one automatically and this link keeps working.
*/
window.ZAMI_APK_URL = "https://sentfromthefuture.github.io/Music/zami.apk";

// Optional: bump this when you release a new version so the download
// page can display it (purely cosmetic, doesn't affect the link above).
window.ZAMI_APK_VERSION = "1.0.0";
